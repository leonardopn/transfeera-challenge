generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

model Receiver {
    id             String @id
    completed_name String
    email          String
    cpf_cnpj       String
    status         String
    pix_key_type   String
    pix_key        String
}
